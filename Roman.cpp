#include<bits/stdc++.h>
#define F first
#define S second
#define PB push_back
#define MP make_pair
#define REP(i,a,b) for(int i=a;i<b;i++)
typedef long long ll;
using namespace std;

// std::vector<char>v;
//std::vector<pair<int,int>>v;
//std::vector<tuple<int,int,int>>v;
bool check(int x,int y)
{
    if(sqrt(pow(x,2)+pow(y,2))==(int))
    {
        
    }
}
void solve()
{
    int x,y;
    if(check(x,y)==true)
    {

    }
    else{

    }
    
}
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);
    ll t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    return 0;
}